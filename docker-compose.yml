services:
  digest:
    build: .
    container_name: clippings
    restart: unless-stopped
    volumes:
      - ./config.yaml:/app/config.yaml
      - ./data:/app/data
    environment:
      - TZ=UTC
    ports:
      - "3000:8000"
